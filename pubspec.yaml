name: broken_tree
description: Repro for broken tree selection

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  angular: ^6.0.0
  angular_components: ^1.0.1

dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.11.0
  pedantic: ^1.9.0
